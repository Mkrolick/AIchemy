# Ralph-Loop Refinement Log — `aichemy-pricing` plans

Each line: `[iter N] <sub-plan> — <one-line description>`.

- [iter 1] B-resolvers — fixed header drift: deps line referenced private `_iter_sdf_records` but implementation uses public `iter_sdf_records`; clarified the module is private but the function is module-public.
- [iter 2] A-foundation — fixed conftest live-marker filter bug: `"live" in markexpr` falsely matched `-m "not live"` (substring match). Replaced with regex `(?<!not )\blive\b` so `-m "not live"` correctly excludes live tests.
- [iter 3] C-tier1-vendors — added CNY/¥ support to MolbaseVendor: per CLAIM-18 majority of suppliers are Chinese, and old regex only matched USD. New regex captures currency token; added `_TOKEN_TO_CURRENCY` map for USD/CNY/EUR/GBP. Added test for CNY parsing with ¥ symbol. Test counts updated in C, E, master.
- [iter 4] E-cli-integration — closed `_InchikeyAdapter` placeholder: implemented inline with RDKit InChIKey computation, static FX-to-USD table dated 2026-04-25 (USD/GBP/EUR/CNY/JPY/SEK), warning log on missing FX rate, lazy rdkit import, and a fall-back to StubPriceLookup when no SDFs are present in `catalog_dir`. Self-review updated.
- [iter 5] B-resolvers — hardened ZINC tranche resolver against column-layout drift: replaced fixed-position parser (assumed cols 0=smiles, 1=zinc_id, 2=inchikey, 3=vendor) with regex-based scan (`[A-Z]{14}-[A-Z]{10}-[A-Z]` for InChIKey, `vendor:code` shape for vendor field) so the resolver works regardless of cohort column ordering. Strengthened live test from "no crash" to "≥1 hit when fixture has tabs" so a silently-empty index can no longer hide a parser-format mismatch. Added 2 unit tests (alternate column order; rejects malformed IK). B: 14→16; total: 75→77.
- [iter 6] D-tier2-3-vendors — Enamine D1 placeholder hardening: discovered via JS-bundle inspection that the actual product backend host is `ebc.enamine.net` (not `enaminestore.com`); the customer-facing site is a thin shell. Added "Pre-discovery facts" section to D1.0 with the bundle finding, the host, and a fallback path (HTML-parsing if no JSON XHR is observed — covers RSC case). Hardened `EnamineVendor.__init__` to raise `NotImplementedError` if `_API_URL` is still the discovery placeholder, so the placeholder can't accidentally ship and silently return None forever.
- [iter 7] D-tier2-3-vendors — MedChemExpress fixture capture validation: old script wrote `mce_acetyl_coa.html` regardless of status_code (a 403 Cloudflare challenge would silently poison the test fixture; subsequent tests would assert against the CF challenge HTML and pass for the wrong reason). Replaced with a validating Python heredoc that checks (a) status==200, (b) body ≥ 5KB, (c) absence of CF challenge markers ("Just a moment...", "cf-browser-verification", "challenge-platform", "Checking your browser", "Enable JavaScript and cookies"), (d) presence of the product name. Exits non-zero with diagnostic messages if any check fails — placeholder fixture cannot ship.
- [Revision 8] Switched to Claude tasks (#1–#15). Factored shared `_capture.py` validator into Sub-Plan A Task A7. Updated all six vendor fixture-capture steps to call it: Fluorochem, Molbase, Tocris (sub-plan C); ChemCruz, MedChemExpress (sub-plan D, replacing the iter-7 inline heredoc with a single helper call); plus a streaming-aware variant for sub-plan B PubChem (gunzip pipe → awk → grep validation, since the helper would buffer the full 60MB shard in memory). Now every captured fixture must pass status/min-size/bad-marker/required-marker checks before being written. Single source of truth for capture validation, cuts ~40 lines of duplicated logic across the plans.
- [Revision 9] CLI `chain` command — dropped private `chain.inner.members` access. New impl iterates `_VENDORS.items()` directly (no cache; it's an ad-hoc debug command). Also cleaned up `chain.lookup(...)` in the integration test to drop the `# type: ignore[attr-defined]` on `chain.inner.lookup(...)`.
- [Revision 10] FX-table completeness — added integration test `test_fx_table_covers_every_currency_literal` that asserts `set(typing.get_args(Currency)) ⊆ set(_FX_TO_USD_AS_OF_2026_04_25)`, so adding a new Currency literal member without extending the FX table will fail loudly. E: 11→12 tests; total: 77→78.
- [Revision 11] PubChem capture — subsumed by Revision 8's streaming validator; the bandwidth cost is unchanged but now documented and bounded by awk-exit's SIGPIPE.
- [Revision 12] Pre-commit compatibility — added an explicit Pre-commit note to Sub-Plan A's tech-stack section pointing out that ruff/ruff-format may auto-reformat staged files and require re-staging. Forbids `--no-verify`.
- [Revision 13] Master-plan de-duplication — collapsed Phases 0–8 (1500 lines of task content already living in sub-plans A–E) into a 9-line summary table. Master is now 130 lines (was 1649); single source of truth for tasks lives in the sub-plan files.
- [Revision 14] Currency-handling audit — confirmed each vendor's hard-coded currency matches its real pages: Fluorochem GBP, Tocris/Cayman/ChemCruz/MCE USD, Enamine + Molbase read currency dynamically. Added an audit-finding note to sub-plan C's self-review.
- [Revision 15] End-to-end coherence read-through — verified DAG is sound (A → B/C/D parallel → E), all inter-plan imports resolve via the public `iter_sdf_records` name (post-Revision 1), and cumulative test counts (78 offline + 8 live) match across master + E summary tables.
