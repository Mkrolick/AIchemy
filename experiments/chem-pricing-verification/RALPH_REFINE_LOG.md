# Ralph-Loop Refinement Log — `aichemy-pricing` plans

Each line: `[iter N] <sub-plan> — <one-line description>`.

- [iter 1] B-resolvers — fixed header drift: deps line referenced private `_iter_sdf_records` but implementation uses public `iter_sdf_records`; clarified the module is private but the function is module-public.
- [iter 2] A-foundation — fixed conftest live-marker filter bug: `"live" in markexpr` falsely matched `-m "not live"` (substring match). Replaced with regex `(?<!not )\blive\b` so `-m "not live"` correctly excludes live tests.
- [iter 3] C-tier1-vendors — added CNY/¥ support to MolbaseVendor: per CLAIM-18 majority of suppliers are Chinese, and old regex only matched USD. New regex captures currency token; added `_TOKEN_TO_CURRENCY` map for USD/CNY/EUR/GBP. Added test for CNY parsing with ¥ symbol. Test counts updated in C, E, master.
- [iter 4] E-cli-integration — closed `_InchikeyAdapter` placeholder: implemented inline with RDKit InChIKey computation, static FX-to-USD table dated 2026-04-25 (USD/GBP/EUR/CNY/JPY/SEK), warning log on missing FX rate, lazy rdkit import, and a fall-back to StubPriceLookup when no SDFs are present in `catalog_dir`. Self-review updated.
- [iter 5] B-resolvers — hardened ZINC tranche resolver against column-layout drift: replaced fixed-position parser (assumed cols 0=smiles, 1=zinc_id, 2=inchikey, 3=vendor) with regex-based scan (`[A-Z]{14}-[A-Z]{10}-[A-Z]` for InChIKey, `vendor:code` shape for vendor field) so the resolver works regardless of cohort column ordering. Strengthened live test from "no crash" to "≥1 hit when fixture has tabs" so a silently-empty index can no longer hide a parser-format mismatch. Added 2 unit tests (alternate column order; rejects malformed IK). B: 14→16; total: 75→77.
- [iter 6] D-tier2-3-vendors — Enamine D1 placeholder hardening: discovered via JS-bundle inspection that the actual product backend host is `ebc.enamine.net` (not `enaminestore.com`); the customer-facing site is a thin shell. Added "Pre-discovery facts" section to D1.0 with the bundle finding, the host, and a fallback path (HTML-parsing if no JSON XHR is observed — covers RSC case). Hardened `EnamineVendor.__init__` to raise `NotImplementedError` if `_API_URL` is still the discovery placeholder, so the placeholder can't accidentally ship and silently return None forever.
- [iter 7] D-tier2-3-vendors — MedChemExpress fixture capture validation: old script wrote `mce_acetyl_coa.html` regardless of status_code (a 403 Cloudflare challenge would silently poison the test fixture; subsequent tests would assert against the CF challenge HTML and pass for the wrong reason). Replaced with a validating Python heredoc that checks (a) status==200, (b) body ≥ 5KB, (c) absence of CF challenge markers ("Just a moment...", "cf-browser-verification", "challenge-platform", "Checking your browser", "Enable JavaScript and cookies"), (d) presence of the product name. Exits non-zero with diagnostic messages if any check fails — placeholder fixture cannot ship.
- [Revision 8] Switched to Claude tasks (#1–#15). Factored shared `_capture.py` validator into Sub-Plan A Task A7. Updated all six vendor fixture-capture steps to call it: Fluorochem, Molbase, Tocris (sub-plan C); ChemCruz, MedChemExpress (sub-plan D, replacing the iter-7 inline heredoc with a single helper call); plus a streaming-aware variant for sub-plan B PubChem (gunzip pipe → awk → grep validation, since the helper would buffer the full 60MB shard in memory). Now every captured fixture must pass status/min-size/bad-marker/required-marker checks before being written. Single source of truth for capture validation, cuts ~40 lines of duplicated logic across the plans.
- [Revision 9] CLI `chain` command — dropped private `chain.inner.members` access. New impl iterates `_VENDORS.items()` directly (no cache; it's an ad-hoc debug command). Also cleaned up `chain.lookup(...)` in the integration test to drop the `# type: ignore[attr-defined]` on `chain.inner.lookup(...)`.
- [Revision 10] FX-table completeness — added integration test `test_fx_table_covers_every_currency_literal` that asserts `set(typing.get_args(Currency)) ⊆ set(_FX_TO_USD_AS_OF_2026_04_25)`, so adding a new Currency literal member without extending the FX table will fail loudly. E: 11→12 tests; total: 77→78.
- [Revision 11] PubChem capture — subsumed by Revision 8's streaming validator; the bandwidth cost is unchanged but now documented and bounded by awk-exit's SIGPIPE.
- [Revision 12] Pre-commit compatibility — added an explicit Pre-commit note to Sub-Plan A's tech-stack section pointing out that ruff/ruff-format may auto-reformat staged files and require re-staging. Forbids `--no-verify`.
- [Revision 13] Master-plan de-duplication — collapsed Phases 0–8 (1500 lines of task content already living in sub-plans A–E) into a 9-line summary table. Master is now 130 lines (was 1649); single source of truth for tasks lives in the sub-plan files.
- [Revision 14] Currency-handling audit — confirmed each vendor's hard-coded currency matches its real pages: Fluorochem GBP, Tocris/Cayman/ChemCruz/MCE USD, Enamine + Molbase read currency dynamically. Added an audit-finding note to sub-plan C's self-review.
- [Revision 15] End-to-end coherence read-through — verified DAG is sound (A → B/C/D parallel → E), all inter-plan imports resolve via the public `iter_sdf_records` name (post-Revision 1), and cumulative test counts (78 offline + 8 live) match across master + E summary tables.

--- ultrareview round 1 (PR #8) findings → Revisions 16-21 ---

- [Revision 17] A-foundation — bug_021 (normal): added `try/except Exception` + `log.warning` + `continue` around `m.lookup(ref)` in ChainedPriceLookup, mirroring the existing aichemy.preprocessing.augment.prices.ChainedPriceLookup contract ("one source failing shouldn't kill the chain"). Without this, one transient httpx.ConnectError aborts the entire augment_prices dict-comp. Added test `test_chain_swallows_per_member_exceptions_and_continues`. A: 20→21 offline tests.
- [Revision 19] A-foundation — bug_005 (normal): removed bogus Step 4 that created a parallel `[project.optional-dependencies] dev` block (would conflict with existing PEP 735 `[dependency-groups] dev` in pyproject.toml; would downgrade pytest-httpx >=0.36 to >=0.30; would lose pre-commit/nbstripout/dvc from synced env). Replaced with a verify-only step pointing at the existing dev group. Step 5 now uses `uv sync --extra pricing --group dev`.
- [Revision 20] A-foundation — bug_014 (nit): live-marker filter now normalizes whitespace + strips parens before applying the `(?<!not )\\blive\\b` regex, so `'not (live)'`, `'not(live)'`, `'not  live'` all canonicalize to `'not live'` and route through the lookbehind correctly. Closes the fail-open silent-network-test path.
- [Revision 21] B-resolvers — bug_015 (nit): ZINC tranche fixture capture rerouted through `_capture.py` (Sub-Plan A Task A7) with required-markers (tab character + `ZINC` token). Old `curl -sL ... | head | > foo.smi || echo` pipeline silently wrote zero-byte or HTML-404 fixtures that the live test then skipped or passed vacuously.
- [Revision 18] C/D — merged_bug_007 (normal): vendor regex parsers were matching molecular-weight tokens (`308.4 g/mol`) as pack sizes, paired with the first $price → 3+ orders of magnitude wrong prices cached for 30 days. Added `strip_molarity_tokens()` helper to `_common.py` that pre-strips `g/mol`, `mg/mL`, `mg/kg`, `g/L` patterns. Wired into Tocris, Molbase, MedChemExpress. Added `test_tocris_does_not_match_molecular_weight_as_pack_size` regression test injecting realistic MW prose ahead of pack table. C: 14→15 offline tests.
- [Revision 16] D/E — merged_bug_002 (normal): `build_default_chain` was eagerly instantiating EnamineVendor (which Revision 6's fail-loud guard makes raise NotImplementedError until manual D1.0 discovery) → all E2 tests + integration test failed. Fix: factored vendor classes into `_DEFAULT_VENDOR_CLASSES`, wrapped each instantiation in try/except NotImplementedError + log.warning, so chain factory degrades gracefully with placeholder-skipping. ALSO inlined the verbatim Enamine fail-loud guard pattern in CaymanVendor D2.1 Step 2 (was prose-only "same shape as EnamineVendor" — silent-Cayman failure mode possible). Added `test_build_default_chain_skips_placeholder_vendors_gracefully`. E: 12→13 offline tests.

Cumulative test counts post-Revisions-16-21: A 21, B 16+1, C 15+3, D 16+4, E 13 = **81 offline + 8 live**.

--- ultrareview round 2 (PR #8 head c571389) findings → Revisions 22-28 ---

- [Revision 22] B-resolvers — bug_010 (normal): iter_sdf_records opened files with path.open("rt", errors="replace") with no gzip handling. Sub-plan E's catalog glob is "*.sdf*" (matches .sdf.gz). Reading deflate bytes as UTF-8 with replace silently yielded zero records → every price lookup returns None for the wrong reason. Added gzip.open detection by .gz suffix, plus regression test using a gzipped fixture. B: 16→17 offline tests.
- [Revision 23] E-cli-integration — bug_001 (normal): PricesConfig declares Literal["stub","chained"] + extra="forbid"; sub-plan E's YAML/make_lookup edits would fail at config load with ValidationError on every aichemy CLI command. Added Step 1b that updates src/aichemy/config.py FIRST: new AichemyPricingConfig BaseModel + Literal extended to include "aichemy_pricing" + typed aichemy_pricing field on PricesConfig. Then YAML/make_lookup edits become valid.
- [Revision 24] A-foundation — bug_002 (normal): three pyproject.toml blocks scoped to src/aichemy only — [tool.hatch.build.targets.wheel].packages, [tool.mypy].files, [tool.pytest.ini_options].testpaths. Wheel ships console script but not module → ModuleNotFoundError on fresh install. Added Step 3b extending all three to include src/aichemy_pricing, plus an `unzip -l` smoke check for the wheel.
- [Revision 25] A-foundation — bug_011 (nit): test counts disagreed across 3 sections — file-structure block (3+1+2+2+2=10), summary table (21), expected-output line (20). Updated file-structure to (7),(3),(4),(4),(3) and bumped expected output to "21 passed". 21 was canonical (matches code blocks + R17).
- [Revision 26] E-cli-integration — bug_004 (nit): _InchikeyAdapter.lookup didn't catch RDKit hard exceptions from MolToInchiKey (Boost.Python.ArgumentError, RuntimeError on radicals/non-standard valences). One bad SMILES would abort augment_prices' dict-comp. Added try/except + log.warning + return None, mirroring R17's chain-defense pattern.
- [Revision 27] E-cli-integration — merged_bug_003 (nit): CLI `lookup` invoked _VENDORS[vendor]() with no try/except → bare Python traceback when EnamineVendor/CaymanVendor placeholder guard fires. Asymmetric with build_default_chain (R16) and chain CLI (R9). Added try/except NotImplementedError + typer.Exit(2) with placeholder message + regression test. E: 13→14 offline tests.
- [Revision 28] E-cli-integration — bug_005 (nit): static FX table had no freshness guard. Combined with 30-day quote cache, rates compound silent drift over months/years (CNY 5-10% intra-year). Added _FX_AS_OF: date constant + _FX_MAX_AGE = 120 days + _check_fx_freshness() module-init function that emits log.warning when stale.

Cumulative test counts post-Revisions-22-28: A 21, B 17+1, C 15+3, D 16+4, E 14 = **83 offline + 8 live**.
